{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -lc 'D=\"${DATA_DIR:-/data}\"; find \"$D/deliveries\" -name \"*.part\" -delete 2>/dev/null; find \"$D\" -name \"*.tmp\" -delete 2>/dev/null; df -h \"$D\" 2>&1 || true; exec uvicorn main:app --host 0.0.0.0 --port $PORT'",
    "healthcheckPath": "/docs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
